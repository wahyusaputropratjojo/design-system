---
title: Button
description: A clickable control that allows a user to directly command the application to perform a specific function, such as confirming a choice or starting a process.
---

import { Tab, Tabs, TabsList, TabsTrigger } from "fumadocs-ui/components/tabs";
import { Button } from "@/components/base/button";
import { ArrowLeftIcon, ArrowRightIcon, CodeIcon, EyeIcon } from "lucide-react";
import { PreviewCard } from "@/components/preview-card";
import { File, Folder, Files } from 'fumadocs-ui/components/files';
import { ReactIcon } from "@/assets/icons/react-icon";
import { AdobeIcon } from "@/assets/icons/adobe-icon";
import { TypeTable } from 'fumadocs-ui/components/type-table';
import Link from "next/link"

{
  <PreviewCard>
    <Button>Button</Button>
  </PreviewCard>
}

---

## Installation
This component has a dependency on the following component. Make sure to install it before proceeding.
<Cards>
  <Card
    icon={<ReactIcon />}
    href="/components/spinner"
    title="Spinner"
  >
    Component
  </Card>
</Cards>

Install the following dependencies.

```bash tab="npm" title="Terminal"
npm install react-aria-components tailwind-variants
```

```bash tab="pnpm" title="Terminal"
pnpm add react-aria-components tailwind-variants
```

```bash tab="bun" title="Terminal"
npm install react-aria-components tailwind-variants
```

Copy and paste the following code into `button.tsx`.

<Files>
  <Folder name="components" defaultOpen disabled>
    <Folder name="base" defaultOpen disabled>
      <File name="button.tsx" icon={<ReactIcon />} />
    </Folder>
  </Folder>
</Files>

```tsx lineNumbers title="button.tsx"
"use client";

import type { Ref } from "react";
import type { ButtonProps as ButtonPrimitiveProps } from "react-aria-components";
import { Button as ButtonPrimitive, composeRenderProps } from "react-aria-components";
import type { VariantProps } from "tailwind-variants";
import { Spinner } from "@/components/base/spinner";
import { tv } from "@/lib/utils/tailwind-variants";

const buttonVariants = tv({
  base: "relative isolate inline-flex cursor-pointer pending:cursor-not-allowed select-none items-center justify-center gap-x-(--gap) rounded-full px-(--padding) pending:opacity-50 outline-0 outline-transparent outline-offset-2 transition-colors focus-visible:z-10 focus-visible:outline-2 focus-visible:outline-neutral-12 disabled:cursor-not-allowed disabled:opacity-50 has-[>svg:last-child]:pr-[calc(var(--padding)-calc(var(--gap)/2))] has-[>svg:first-child]:pl-[calc(var(--padding)-calc(var(--gap)/2))] [&_svg]:size-(--icon-size) [&_svg]:shrink-0",
  compoundVariants: [
    {
      className:
        "bg-danger-3 pressed:bg-danger-4 text-danger-9 hover:bg-danger-4",
      intent: "secondary",
      status: "danger",
    },
    {
      className: "size-8",
      isIconOnly: true,
      size: "sm",
    },
    {
      className: "size-10",
      isIconOnly: true,
      size: "md",
    },
    {
      className: "size-12",
      isIconOnly: true,
      size: "lg",
    },
  ],
  defaultVariants: {
    intent: "primary",
    size: "md",
  },
  variants: {
    fullWidth: {
      true: "w-full!",
    },
    intent: {
      primary:
        "bg-primary-9 pressed:bg-primary-10 text-light hover:bg-primary-10",
      secondary:
        "bg-neutral-3 pressed:bg-neutral-4 text-primary-9 hover:bg-neutral-4",
      tertiary:
        "bg-neutral-3 pressed:bg-neutral-4 text-neutral-12 hover:bg-neutral-4",
    },
    isIconOnly: {
      true: "p-0!",
    },
    size: {
      lg: "h-12 text-label-16 [--gap:--spacing(2.5)] [--icon-size:--spacing(5)] [--padding:--spacing(6)]",
      md: "h-10 text-label-14 [--gap:--spacing(2)] [--icon-size:--spacing(4)] [--padding:--spacing(5)]",
      sm: "h-8 text-label-12 [--gap:--spacing(1.5)] [--icon-size:--spacing(4)] [--padding:--spacing(4)]",
    },
    status: {
      danger:
        "border-danger-7 pressed:border-danger-8 bg-danger-9 pressed:bg-danger-10 text-white hover:border-danger-8 hover:bg-danger-10",
    },
  },
});

type ButtonProps = {
  ref?: Ref<HTMLButtonElement>;
  className?: string;
} & Omit<ButtonPrimitiveProps, "className"> &
  VariantProps<typeof buttonVariants>;

export function Button({
  size,
  ref,
  intent,
  status,
  isIconOnly,
  fullWidth,
  className,
  ...props
}: ButtonProps) {
  return (
    <ButtonPrimitive
      className={buttonVariants({
        className,
        fullWidth,
        intent,
        isIconOnly,
        size,
        status,
      })}
      ref={ref}
      {...props}
    >
      {composeRenderProps(props.children, (children, { isPending }) => (
        <>
          {isPending && <Spinner />}
          {children}
        </>
      ))}
    </ButtonPrimitive>
  );
}

```

---

## Usage

### Basic
A simple button.

{
  <PreviewCard>
    <Button>Button</Button>
  </PreviewCard>
}

```tsx title="Code"
<Button>Button</Button>
```

### Intent
The `intent` prop defines the button's visual style. Use `primary` for the main action, and `secondary` for less prominent actions.

{
  <PreviewCard>
    <Button intent="primary">Button</Button>
    <Button intent="secondary">Button</Button>
    <Button intent="tertiary">Button</Button>
  </PreviewCard>
}

```tsx title="Code"
// [!code word:intent]
<Button intent="primary">Button</Button>
<Button intent="secondary">Button</Button>
<Button intent="tertiary">Button</Button>
```

### Status
The `status` prop applies a semantic color to the button, such as `danger` for destructive actions.

{
  <PreviewCard>
    <Button status="danger" intent="primary">Button</Button>
    <Button status="danger" intent="secondary">Button</Button>
  </PreviewCard>
}

```tsx title="Code"
// [!code word:status]
<Button status="danger" intent="primary">Button</Button>
<Button status="danger" intent="secondary">Button</Button>
```

### Size
The `size` prop adjusts the button's height and padding. The default size is `md`.

{
  <PreviewCard>
    <Button size="sm">Button</Button>
    <Button size="md">Button</Button>
    <Button size="lg">Button</Button>
  </PreviewCard>
}

```tsx title="Code"
// [!code word:size]
<Button size="sm">Button</Button>
<Button size="md">Button</Button>
<Button size="lg">Button</Button>
```

### With Icon
Add icons before or after the button's text. The component will automatically handle the spacing.

{
  <PreviewCard>
    <Button>
      <ArrowLeftIcon />
      Button
    </Button>
    <Button>
      Button
      <ArrowRightIcon />
    </Button>
    <Button>
      <ArrowLeftIcon />
      Button
      <ArrowRightIcon />
    </Button>
  </PreviewCard>
}

```tsx title="Code"
// [!code word:ArrowLeftIcon]
// [!code word:ArrowRightIcon]
<Button><ArrowLeftIcon />Button</Button>
<Button>Button<ArrowRightIcon /></Button>
<Button><ArrowLeftIcon />Button<ArrowRightIcon /></Button>
```

### Icon Only
Use the `isIconOnly` prop for buttons that only contain an icon. This adjusts the padding and size to be perfectly square.

{
  <PreviewCard>
    <Button isIconOnly size="sm">
      <ArrowRightIcon />
    </Button>
    <Button isIconOnly size="md">
      <ArrowRightIcon />
    </Button>
    <Button isIconOnly size="lg">
      <ArrowRightIcon />
    </Button>
  </PreviewCard>
}

```tsx title="Code"
// [!code word:isIconOnly]
<Button isIconOnly size="sm"><ArrowRightIcon /></Button>
<Button isIconOnly size="md"><ArrowRightIcon /></Button>
<Button isIconOnly size="lg"><ArrowRightIcon /></Button>
```

### Disabled
The `isDisabled` prop can be used to disable the button.

{
  <PreviewCard>
    <Button isDisabled intent="primary">Button</Button>
    <Button isDisabled intent="secondary">Button</Button>
  </PreviewCard>
}

```tsx title="Code"
// [!code word:isDisabled]
<Button isDisabled intent="primary">Button</Button>
<Button isDisabled intent="secondary">Button</Button>
```

### Pending
The `isPending` prop indicates a loading state. It disables the button and displays a spinner.

{
  <PreviewCard>
    <Button isPending intent="primary">Button</Button>
    <Button isPending intent="secondary">Button</Button>
  </PreviewCard>
}

```tsx title="Code"
// [!code word:isPending]
<Button isPending intent="primary">Button</Button>
<Button isPending intent="secondary">Button</Button>
```
